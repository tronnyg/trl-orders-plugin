name: NOrder
version: ${project.version}
main: com.notpatch.nOrder.NOrder
api-version: '1.21'
folia-supported: true

loader: com.notpatch.nOrder.LibraryLoader

dependencies:
  server:
    Vault:
      load: BEFORE
      required: true
    PlaceholderAPI:
      load: BEFORE
      required: false
    LuckPerms:
      load: BEFORE
      required: false
    ItemsAdder:
      load: BEFORE
      required: false
